class Solution {
    public int maxProduct(int[] nums) 
    {
        int ans=Integer.MIN_VALUE;
        int tempans=1;
        for (int i : nums) {
            tempans*=i;
            
        }
 
        
    }
}